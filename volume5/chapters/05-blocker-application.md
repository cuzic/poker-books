# 第5章　ブロッカー実戦選択

<!-- markdownlint-disable MD026 MD033 MD036 MD040 MD060 -->

> **本章のゴール**: 本書巻� 第7章で学んだブロッカーの基礎を踏まえ、リバーでの具体的なコンボ選択フローを習得する。α式・後手スコアが示した方針を、ブロッカーでコンボ単位に絞り込む技術を身につける。

---

## 5-1 ブロッカーの立ち位置 ─ 4ステップ階層の最終層

リバーでの判断は4つの層で構成されます。ブロッカーはその最終層です。

```
ステップ1（本書 巻� 第9章）: α式でバリューとブラフの比率を決める
  α = ベット額 ÷ (ポット + ベット額)
  → 打てるブラフの総数を設計

ステップ2（本書 巻� 第11章）: 後手スコアでコール/CR/フォールドを決める
  後手スコア = HandScore + A − 3 − C − M
  → 守備側が何%のレンジを守るかを個別ハンドで判断

ステップ3（本書 巻� 第12章〜）: 具体的な戦術を実行する
  → IP/OOPのアクション、サイズ選択、CR条件を判断

ステップ4（本章）: 同強度のコンボ群の中からブロッカーで最終選択する
  → ブラフ候補: バリューブロッカーが強いコンボを優先
  → コール候補: バリューブロッカー保持は強化、ブラフブロッカー保持は抑制
```

ここで最も重要なことを最初に述べます。**ブロッカーは方針を変えない**、という点です。

α式で「ブラフを打てる余地がある」と判断した上でブラフコンボを選ぶ際に、同強度のコンボ群の中でブロッカーの強いものを選ぶのがブロッカーの正しい使い方です。α式が「ブラフは不要」と示している局面で「バリューブロッカーがあるからブラフを打てる」という逆転は誤りです。

本書巻� 第7章でブロッカーの基礎概念（バリューブロッカー・ブラフブロッカー・ダブルブロッカー）を学びました。本章ではそれを実戦に展開します。

---

## 5-2 ブラフ候補の優先順位 ─ バリューブロッカーで選ぶ

先手がブラフを打つ場面で、複数のブラフ候補コンボがある場合、どのコンボを選ぶかをブロッカーで決めます。

**基本原則: 相手のバリューコールレンジの中心をブロックするコンボを選ぶ**

相手がコールしてくるハンドの中心が何かを3秒で特定し、そのキーカードを自分が持つコンボをブラフとして選びます。

### フラッシュ完成ボードでのブラフ選択

**ボード**: K♥T♥2♣A♥9♥（ハート4枚、フラッシュが「完成している環境」）
**α式の計算**: 75%ポットサイズでブラフを打てる方針（方針はStep1で確定済み）

ブラフ候補コンボ（いずれもショーダウン価値が低い）:

| コンボ | ブロッカー効果 | 評価 |
|---|---|---|
| A♥Q♣ | A♥でナッツフラッシュ（A♥x♥）を完全ブロック | 最優先 |
| A♣Q♦ | A♣はハートのブロッカーにならない。Aで非フラッシュバリューをブロック | 次点 |
| K♠J♦ | K♠でボードのK♥と重複なし。K系バリューをブロック | 限定的 |
| 7♦6♣ | ブロッカーほぼなし | 最下位 |

役判定の確認（A♥Q♣ + K♥T♥2♣A♥9♥）: A♥はボードにあり手にあるため同カードの使用不可。このボード設定ではA♥がボードとハンド両方に存在することになり、デッキの整合性に注意が必要です。ハンドを A♦Q♣ に変更して考えます。A♦Q♣ + K♥T♥2♣A♥9♥: ハートの枚数は手に0枚 + ボードに4枚 = 4枚。フラッシュ不成立（5枚必要）。役なしハイカード（AQハイ）。HandScore=0。

一方、A♥Q♦を持つ場合: A♥はボードに既に存在するため、このコンボは使えません。

実際にブラフとして使えるコンボで、ナッツフラッシュ（A♥含み）をブロックするには、自分が「もう1枚のハート」ではなく「A♥そのもの」を持つ必要があります。フラッシュが完成しているボードでA♥がボードに落ちている場合、「A♥を手に持つ」という状況は発生しません。

実際には、ターンまでの進行でA♥がボードになかった段階（フロップ・ターン）でA♥を手に持っていれば、ブラフとしての機能を果たせます。本ボードでA♥がボードに含まれる以上、A♥ブロッカーは手牌では使えません。代わりに「非フラッシュのバリュー（AK・AQ等）をブロックするA」を持つコンボが次善となります。

### 非フラッシュボードでのブラフ選択

**ボード**: K♠7♦2♣A♥Q♦（散開rainbow、フラッシュなし）
**α式の計算**: 50%ポットサイズでブラフを打てる方針

ブラフ候補コンボ（いずれも外れたドロー等、役なし）:

| コンボ | ブロッカー効果 | 評価 |
|---|---|---|
| A♣J♣ | A♣で相手のAK・AQ等のバリューをブロック | 優先 |
| Q♣J♣ | Q♣で相手のQQ・KQ等をブロック | 次点 |
| J♦T♣ | このボードで特定のブロッカー効果なし | 最下位 |

このボード（K・A・Q・7・2）での相手のバリューの中心はAK・KQ・AQ・AA・KK・QQ等です。AとQを持つコンボが相手のバリューを最も削れます。J♦T♣はこのボードで「Jがボードに絡まず、Tもボードに絡まない」ため有効なブロッカーを持てません。

---

## 5-3 コール判断でのブロッカー ─ バリューブロッカーとブラフブロッカー

後手スコアで「コール圏内のボーダーライン」と判断したハンドに対して、ブロッカーが判断を微調整します。

### バリューブロッカー ─ コールを強化する

相手のバリューのキーカードを自分が持つ場合、相手のバリュー密度が下がります。同じ後手スコアでも、コールの期待値がわずかに上昇します。

**例: フラッシュ完成ボードでのコール判断**

**ボード**: K♥T♥2♣5♥9♥（ハート4枚、フラッシュ完成環境）
**相手**: 75%ポットをベット
**BBのハンド**: A♦8♦（Aのペアなし、A high）

役判定: A♦8♦ + K♥T♥2♣5♥9♥。ランク: A, 8, K, T, 2, 5, 9。5連続なし。ハートの枚数: 手に0枚 + ボードに4枚 = 4枚でフラッシュ不成立。役なし（Aハイ）。HandScore=0。

```
後手スコア = 0 + 1 − 3 − 7 − 0 = −9 → 明確なフォールド
```

この例ではスコアが大幅にマイナスなため、ブロッカーで調整できる範囲を超えています。ブロッカーはボーダーライン付近のハンドにのみ有効です。

**例: ボーダーラインでのブロッカー確認**

**ボード**: A♠K♦Q♣J♠2♣（散開ほぼドライ、A=3）
**相手**: 75%ポットをベット（C=7）
**BBのハンド1**: Q♥T♦（Qのペア+Tのブロッカー）

役判定: Q♥T♦ + A♠K♦Q♣J♠2♣。Qが2枚（Q♥とQ♣）、A・K・J・2が各1枚。ボードのペア（Q♣）と手のQ♥でQのペア（Q's）。Tは手のみで1枚。best 5 = A-K-Q-Q-J（2ペアではなく1ペア、Qのペア、AKJキッカー）。HandScore=10（トップペアか2番目のペアかで判断、Q♥でQのペアはセカンドペア的だが、AKQJボードでQのペアはボードの3番目のランク。便宜上HandScore=10）。

```
後手スコア = 10 + 3 − 3 − 7 − 0 = 3 → コール（薄い）
```

スコア=3でコール圏内です。ブロッカー確認: Q♥でQxのバリュー（QQ、AQ、KQ）を1枚ブロック。T♦でストレート（broadway: A-K-Q-J-T）のTを1枚ブロック。相手のバリューの一部が削れており、コール方向に微調整できます。

**BBのハンド2**: 6♦5♦（ブロッカーなし）

役判定: 6♦5♦ + A♠K♦Q♣J♠2♣。ランク: 6, 5, A, K, Q, J, 2。5連続なし（A-K-Q-J-6の連続は4枚止まり）。役なし（Aハイ）。HandScore=0。

```
後手スコア = 0 + 3 − 3 − 7 − 0 = −7 → フォールド
```

6♦5♦はHandScore=0でスコアが大幅マイナス。ブロッカーで調整できる範囲外です。

2つのハンドの比較で明確なのは、「HandScoreが高いハンドを選ぶ」が「ブロッカーで調整する」より圧倒的に重要だということです。ブロッカーはHandScoreが近い複数のコンボを比較する際に機能します。

### ブラフブロッカー ─ コールを抑制する

相手のブラフのキーカードを自分が持つ場合、相手のブラフ候補が物理的に減少します。相手のベットがよりバリューに偏り、コールの期待値が下がります。

**例: 外れたドローのキーカードを自分が持つ**

**ボード**: J♠T♣5♦A♣2♠（リバー、散開ほぼドライ）
**相手**: ポットをベット（C=9）
**BBのハンド**: Q♦9♦（外れたOESD）

役判定: Q♦9♦ + J♠T♣5♦A♣2♠。ランク: Q, 9, J, T, 5, A, 2。5連続確認: A-K-Q-J-T（Kなし）、K-Q-J-T-9（Kなし）、Q-J-T-9-8（8なし）。ストレート不成立。役なし（Aハイ）。HandScore=0。

```
後手スコア = 0 + 3 − 3 − 9 − 0 = −9 → フォールド
```

スコアが大幅マイナスで基本フォールドです。加えてブラフブロッカー確認: Q♦と9♦を持つことで、相手の「外れたQ9系OESDブラフ」が物理的に1コンボ削れます。しかし相手のブラフには Q9 以外の外れたドロー（K9、QT等）も残ります。ブラフブロッカーの効果でさらにフォールド方向が確認されます（ただしこの例ではスコアが既に大幅マイナスのため、ブロッカーが判断を変えるほどの効果はありません）。

---

## 5-4 ダブルブロッカーの強力さ

1枚のカードで複数の相手バリューを同時にブロックするコンボを「ダブルブロッカー」と呼びます。リバーのブラフ選択で特に強力です。

**例: セット・トップペア系が相手のバリューのボードでのブラフ選択**

**ボード**: K♣9♦4♠A♠7♥（散開rainbow、A=3）
**α式計算**: 50%ポットでブラフを打てる方針
**ブラフ候補**（いずれもHandScore=0〜2の弱いハンド）:

| コンボ | ブロッカー | 評価 |
|---|---|---|
| A♦Q♦ | A♦でAK・AA等のバリューをブロック、Q♦でKQ・AQ等をブロック | ダブルブロッカー: 最優先 |
| K♦J♦ | K♦でKK・AK等をブロック、J♦でKJ（タイプによる）をブロック | 単一中心のブロッカー |
| J♦8♦ | ブロッカー弱い | 最下位 |

A♦Q♦は「A系バリュー（AK・AA）」と「Q系バリュー（KQ・AQ）」を同時にブロックするダブルブロッカーです。相手のコールレンジの中心を2方向から削れるため、ブラフとして選ぶ場合の期待値が最も高くなります。

役判定（A♦Q♦ + K♣9♦4♠A♠7♥）: AがボードにA♠あり、手にA♦あり → 異なるカード（問題なし）。ランク: A, Q, K, 9, 4, A, 7。Aが2枚（A♦とA♠）= Aのペア（2 pair よりAのペアが成立）。HandScore = 10〜18（Aのペア、キッカー次第）。実はHandScore=18（TPTK相当）程度になるため、これはブラフ候補ではなくバリューとして扱うべきです。

修正してブラフ候補を再設定します。ブラフに使えるのは「ショーダウン価値が低いハンド」です。同ボードで役なしのコンボ例: J♦T♦（役なし、J-Tはボードの K-9-A-4-7 と5連続なし、HandScore=0）がブラフ候補となります。

| コンボ | ブロッカー | 評価 |
|---|---|---|
| J♦T♦ | JでKJ等をブロック、Tでは限定的 | 弱い |
| Q♦J♦ | QでKQ・AQ等をブロック、Jで限定的 | 中程度 |
| 5♦6♦ | ブロッカーなし | 最下位 |

バリューとブラフの区別を正確に行った上で、ブラフ候補の中でダブルブロッカーを選びます。バリューハンド（AペアやKペア）をブラフとして使うのは期待値を捨てる行為です。

---

## 5-5 ブラフ選択の5秒判断フロー

実戦では長時間の計算が不可能です。5秒以内で判断するためのフローを示します。

```
【ブラフ選択の5秒フロー】

Q1: 複数のブラフ候補コンボがあるか？
  → NO（1種類しかない）: そのコンボでブラフ
  → YES（複数ある）: Q2へ

Q2: 相手のバリューの中心は何か？（3秒で特定）
  フラッシュ完成ボード → ナッツフラッシュ（Ax♥等）
  ドライボード → セット・トップペア（KK・AK等）
  コネクテッドボード → ストレート・2ペア以上

Q3: その「相手のバリュー中心」をブロックするコンボはどれか？
  → ブロックしているコンボを選ぶ
```

この3問を5秒以内で処理します。Q2の「相手のバリュー中心の特定」が最も重要で、これが明確であれば自然にQ3の答えが出ます。

**コール判断の5秒フロー**

```
Q1: 後手スコアで「コール/フォールドの境界付近」（スコア±2）か？
  → NO（明確にコールまたはフォールド）: 後手スコアに従う
  → YES（ボーダーライン）: Q2へ

Q2: 自分は相手のバリューをブロックしているか？
  → YES → コール方向に微調整（コールの期待値がわずかに上昇）
  → NO → 後手スコアの標準に従う

Q3: 自分は相手のブラフをブロックしているか？
  → YES → フォールド方向に微調整（相手のベットがよりバリューに偏る）
  → NO → 後手スコアの標準に従う
```

Q2とQ3は同時に確認します。バリューブロッカーとブラフブロッカーを同時に持つ場合は、どちらの効果が大きいかを直感的に判断します。

---

## 5-6 実戦例 ─ ブロッカーで判断が変わる4場面

### 場面1: フラッシュ外れのブラフ候補選択

**ボード**: K♥T♥2♣5♥Q♦（ハート3枚でフラッシュ未完成リバー）
**α式**: 75%ポットでブラフ打てる方針
**ブラフ候補（いずれもHandScore=0〜2）**:

候補A: A♥J♣（A♥でナッツフラッシュ（A♥x♥）をブロック、Jはこのボードで限定的）

役判定: A♥J♣ + K♥T♥2♣5♥Q♦。ハートの枚数: 手に1枚（A♥）+ ボードに3枚（K♥, T♥, 5♥）= 4枚。フラッシュ不成立（5枚必要）。役なし（AJハイ）。HandScore=0。

候補B: A♦J♣（Aのブロッカーだがハートなし）

役判定: A♦J♣ + K♥T♥2♣5♥Q♦。ハート: 手に0枚 + ボード3枚 = 3枚。フラッシュ不成立。役なし（AJハイ）。HandScore=0。

候補C: J♦8♦（ブロッカーほぼなし）

役判定: J♦8♦ + K♥T♥2♣5♥Q♦。ランク: J, 8, K, T, 2, 5, Q。5連続なし。役なし（KJハイ）。HandScore=0。

ブロッカー比較:
- 候補A（A♥J♣）: A♥でナッツフラッシュを完全ブロック → **最優先**
- 候補B（A♦J♣）: Aで非フラッシュバリュー（AK・AQ等）をブロック → **次点**
- 候補C（J♦8♦）: ブロッカー弱い → **最下位**

ハート3枚ボードでのコールレンジの中心は「フラッシュ（ハート×2のハンド）」です。A♥を持つ候補Aはそのナッツフラッシュを完全に削れるため、ブラフとして最優先されます。

### 場面2: ペアボードでのコールボーダーライン

**ボード**: J♠J♦T♣9♥2♠（Jのペア、A=2）
**相手**: 75%ポットベット（C=7）
**BBのハンド1**: K♠Q♦（役なし、K-Qハイ）

役判定: K♠Q♦ + J♠J♦T♣9♥2♠。Jが2枚（J♠とJ♦）→ ボードのペア。best 5 = K-J-J-Q-T（1ペア、J's、KQキッカー）。HandScore = 8（セカンドペア相当として扱うか、ボードペアのみ=8）。

```
後手スコア = 8 + 2 − 3 − 7 − 0 = 0 → コール（後手下限スコアの境界）
```

スコア=0はコール/フォールドの境界です。ブロッカー確認: K♠でKK・AK等のバリューをブロック（相手のナッツに近いKK、AKが少し削れる）。コール方向への微調整あり。

**BBのハンド2**: 5♠4♠（役なし）

役判定: 5♠4♠ + J♠J♦T♣9♥2♠。ランクはJ, J, T, 9, 5, 4, 2。5連続: T-9-8-7-6（8なし）、9-8-7-6-5（8なし）等不成立。役はボードペア（J-J）のみ → best 5 = J-J-T-5-4（1ペア、J's）。HandScore = 8。

```
後手スコア = 8 + 2 − 3 − 7 − 0 = 0 → コール（境界）
```

同じHandScore=8でも、K♠Q♦はブロッカーあり（コール向き）、5♠4♠はブロッカーなしです。ボーダーラインではK♠Q♦が優先されます。

### 場面3: ダブルブロッカーが明暗を分けるブラフ選択

**ボード**: A♠K♦Q♦J♠2♣（ほぼブロードウェイ、A=2）
**α式**: 50%ポットでブラフを打てる方針

注意: このボードではA-K-Q-J-Tのブロードウェイストレートが完成する可能性があります。T（10）を持つハンドは既にストレートが成立しています。役確認が重要です。

ブラフ候補の中で役なしのコンボを確認します。

候補A: T♥8♦

役判定: T♥8♦ + A♠K♦Q♦J♠2♣。ランク: T, 8, A, K, Q, J, 2。5連続: A-K-Q-J-T（broadway）→ Tがhandにある → **ブロードウェイストレート完成**。HandScore=30（ストレート）。これはブラフ候補ではなくバリューです。

候補B: 8♥7♦

役判定: 8♥7♦ + A♠K♦Q♦J♠2♣。ランク: 8, 7, A, K, Q, J, 2。5連続: A-K-Q-J-8（8が連続しない）、K-Q-J-9-8（9なし）等不成立。役なし（Aハイ）。HandScore=0。ブロッカーなし。

候補C: T♠9♦

役判定: T♠9♦ + A♠K♦Q♦J♠2♣。5連続: A-K-Q-J-T（broadway完成）→ Tがある → **ストレート完成**。HandScore=30。バリュー。

候補D: 9♥8♦

役判定: 9♥8♦ + A♠K♦Q♦J♠2♣。5連続: K-Q-J-T-9（Tなし）。役なし（Aハイ）。HandScore=0。ブロッカー: 9♥でK-Q-J-T-9のTサイドをブロックするわけではなく、9を持つことで「相手の98系のストレートドロー外れのブラフ（98はブロードウェイ未完成だが）」が削れます。効果は限定的。

このボードではTを含むハンドがストレートを完成させるため、ブラフとして使えるのはTを含まないHandScore=0のコンボに限られます。8♥7♦や9♥8♦が候補ですが、どちらもブロッカー効果は限定的です。ブロードウェイ系ボードはブラフ候補が少なく、ブロッカーで差をつけにくい代表例です。

### 場面4: コールのブラフブロッカー確認

**ボード**: Q♠J♦T♥4♣2♠（A=2、コネクテッド少し）
**相手**: 75%ポットベット（C=7）
**BBのハンド**: K♠9♠

役判定: K♠9♠ + Q♠J♦T♥4♣2♠。ランク: K, 9, Q, J, T, 4, 2。5連続: K-Q-J-T-9（全部揃う） → **K-highストレート完成**。HandScore=30（ストレート）。

```
後手スコア = 30 + 2 − 3 − 7 − 0 = 22 → CR推奨
```

スコアが大きくCR域なのでブロッカー確認の前にCRを選びます。これはブロッカーが判断を変えない（強いハンドの場合）の典型例です。

同じ局面でBBのハンドが 8♥7♦ だった場合を確認します。

役判定: 8♥7♦ + Q♠J♦T♥4♣2♠。ランク: 8, 7, Q, J, T, 4, 2。5連続: J-T-9-8-7（9なし）。役なし（QJハイ）。HandScore=0。

```
後手スコア = 0 + 2 − 3 − 7 − 0 = −8 → フォールド
```

大幅マイナスなのでブロッカー確認の必要なくフォールドです。

ブロッカーが機能する「ボーダーライン」のハンド例: A♠3♠

役判定: A♠3♠ + Q♠J♦T♥4♣2♠。ランク: A, 3, Q, J, T, 4, 2。5連続: A-K-Q-J-T（Kなし）。役なし（Aハイ）。HandScore=0。

```
後手スコア = 0 + 2 − 3 − 7 − 0 = −8 → フォールド
```

これも大幅マイナス。このボードではHandScore=0のハンドはすべてフォールドです。

HandScore=8のセカンドペア以上がないとボーダーラインになりません。セカンドペア（例: J♥8♥、JのペアはボードのJ♦と手のJ♥）を確認します。

役判定: J♥8♥ + Q♠J♦T♥4♣2♠。Jが2枚（J♥とJ♦）→ トリップス? いいえ、J♦はボードの1枚、J♥は手の1枚。合計2枚のJ = ペア。best 5 = Q-J-J-T-8（1ペア、J's、QTキッカー）。HandScore=8（ペア）。

```
後手スコア = 8 + 2 − 3 − 7 − 0 = 0 → コール（境界）
```

スコア=0でボーダーライン。ブロッカー確認: J♥でJJ・QJ・KJ・AJ等のバリューを1枚ブロック（バリューブロッカー弱）。また8♥で外れたドロー（98、87等）の一部をブラフブロッカーとして削る効果もわずかにあります。このような境界付近では、相手のプレイスタイルや実戦的な読みを加味した総合判断になります。

---

## 5-7 ブロッカーの限界 ─ 3つの境界線

ブロッカーは補助ツールです。その限界を理解することで、過信を防げます。

### 限界1: レンジの一部しか削れない

ナッツフラッシュをブロックしても、相手のコールレンジにはセカンドナッツフラッシュ・非フラッシュバリュー等が残ります。「ブロッカーがあるから必ずブラフが通る」という思考は誤りです。

```
フラッシュ完成ボードで A♥を持つ場合:
  削れるもの: ナッツフラッシュ（A♥x♥）
  残るもの: セカンドナッツフラッシュ（K♥x♥）
          中位フラッシュ（Q♥x♥〜6♥x♥）
          セット・2ペア等の非フラッシュバリュー
```

ブロッカーは「相手のコールレンジの一部を削る」効果です。削られた後に残るレンジに対してブラフが成功するかどうかが、ブラフの期待値を決めます。

### 限界2: ストーリー整合性がブロッカーより優先

フロップ・ターンのアクションと一貫したストーリーがない場合、ブロッカーが強くても相手はコールしてきます。

フロップでチェック、ターンでチェック、リバーで突然大きなブラフを打つ場合、ブロッカーが機能していても相手は「ストーリーが不自然」と判断してコールしやすくなります。ブロッカーは「良いストーリーの上に乗せる最終調整」です。ストーリーなきブロッカー選択は効果を発揮しません。

### 限界3: 相手がGTOから外れたプレイをしている場合

ブロッカー効果は「相手がGTO的にプレイしている場合」に最大化します。相手がブラフをほとんどしない（タイトなバリューベットのみ）プレイヤーに対しては、バリューブロッカーを使ったブラフは機能しません。相手のレンジのブラフ比率が低い場合、バリューブロッカーで「ナッツを削っても」相手のコールレンジの中心はバリューのままだからです。

逆に、相手が頻繁にブラフをするプレイヤーに対しては、ブラフブロッカーの見落としを特に注意します。相手のブラフレンジのキーカードを自分が持つ場合、コール判断が弱まります。

---

## 5-8 4ステップ階層のまとめと巻� 第12章への橋渡し

本章で整理したブロッカー論を、4ステップ階層全体の中で位置づけます。

**4ステップ階層（再確認）**

```
Step 1（巻� 第9章）: α式 → バリューとブラフの比率を決定
Step 2（巻� 第11章）: 後手スコア → コール/CR/フォールドを決定
Step 3（巻� 第12章〜）: 戦術実行 → 具体的なアクションを選択
Step 4（本章）: ブロッカー → 同強度のコンボ群から最終選択
```

この4ステップは独立しています。各ステップが異なる問いに答えます。

- α式は「何枚のブラフを打てるか」（レンジレベルの問い）
- 後手スコアは「このハンドをコールレンジに含めるか」（ハンドレベルの問い）
- ブロッカーは「同じ役割を果たせる複数のコンボのどれを選ぶか」（コンボレベルの問い）

この粒度の違いを混同すると判断が誤ります。ブロッカーはコンボレベルの最終調整です。

**巻� 第12章への橋渡し**

巻� 第12章からはIPのリバー攻撃（巻� 第12章）、OOPのリバー応戦（巻� 第13章）と、実際のリバー戦術を扱います。これらの章では、α式・後手スコア・ブロッカーの3要素を統合した実戦判断を展開します。

巻� 第11章（後手スコア）で数値化した「守る強さ」と、本章（ブロッカー）で決めた「どのコンボを使うか」という2つの判断を組み合わせることで、巻� 第12章以降の戦術が生きてきます。

---

## ミニクイズ

### 問1

フラッシュ完成リバーボード K♥9♥2♣T♥7♣（ハート4枚）で、あなたはα式でブラフを打てる方針と判断しました。次の2つのブラフ候補コンボのうち、ブロッカー観点で優れているのはどちらですか。両者ともHandScore=0（役なし）です。

1. A♣Q♦（AとQ、ハートなし）
2. A♥Q♣（A♥を保有）

ただし、A♥はボードに含まれていません（ボードのハートはK♥、9♥、T♥、7♥）。

### 問2

後手スコアで「コール境界（スコア±1）」と判断したハンドが2つあります。ブロッカー観点でどちらを選びますか。

ボード A♠K♦Q♣J♠2♣（散開ほぼドライ、A=3）、相手が75%ポットをベット。

ハンド1: Q♥T♦（役はQのペア=QとQ♣で2枚、HandScore=10、後手スコア = 10+3−3−7−0 = 3）

ハンド2: 6♥5♦（役なし=Aハイ、HandScore=0、後手スコア = 0+3−3−7−0 = −7）

1. ハンド1（Q♥T♦）を選ぶ
2. ハンド2（6♥5♦）を選ぶ
3. どちらも後手スコアがマイナスなのでフォールド

### 問3

次のブロッカーの使い方のうち、正しいものはどれですか。

1. バレルスコアが閾値未満（積極バレル不可）でも、ナッツブロッカーを持つならブラフを打てる
2. 後手スコアが明確なフォールド域（スコア−7以下）でも、バリューブロッカーを持つならコールできる
3. 同強度のブラフ候補コンボが複数ある場合、バリューブロッカーが強いコンボを優先してブラフとして選ぶ

---

### 解答

- **問1**: 2（A♥Q♣がブロッカー観点で優れる。ボードにハートが4枚ある場合、相手のコールレンジの中心はフラッシュ（ハート×2のハンド）です。ナッツフラッシュはA♥x♥ですが、A♥がボードにないため手でA♥を保有できます。A♥を持つことで相手のナッツフラッシュコンボ（A♥+他のハート）が物理的にゼロになり、ブラフが通りやすくなります。A♣Q♦はAで非フラッシュバリューをブロックできますが、フラッシュボードでの相手のコールはフラッシュが中心のため、効果が限定的です）

- **問2**: 1（ハンド1のQ♥T♦を選ぶ。後手スコアはハンド1=3（コール）、ハンド2=−7（フォールド）です。ハンド2は後手スコアが明確なフォールド域なのでコールできません。ハンド1はコール域に入っており、さらにQ♥でQxバリュー（AQ・KQ等）をブロック、T♦でストレート（broadway）のTを1枚ブロックというバリューブロッカー効果もあります。「コール境界」と問題文に書かれていますが、実際にはハンド1とハンド2で後手スコアが大きく異なるため、ブロッカーの比較以前に後手スコアで判断が確定しています）

- **問3**: 3（正しい使い方は「同強度のコンボ群の中でバリューブロッカーが強いコンボを優先する」こと。選択肢1はα式やバレルスコアを無視した誤り。ブロッカーは方針を変えない。選択肢2は後手スコアを無視した誤り。スコア−7のような大幅フォールド域はブロッカーで変えられない。ブロッカーが機能するのは「ボーダーライン（スコア±2程度）」のコンボ選択においてのみです）

---

> **本章まとめ**
>
> - ブロッカーは4ステップ階層の最終層。α式・後手スコアが決めた方針を変えず、コンボ単位の選択を微調整する
> - ブラフ選択では「相手のバリューコールレンジの中心をブロックするコンボ」を優先する（バリューブロッカー優先）
> - コール判断では、バリューブロッカー保持でコール方向に微調整、ブラフブロッカー保持でフォールド方向に微調整する
> - ダブルブロッカー（2枚のカードが異なる種類の相手バリューを同時にブロック）は特に強力
> - ブロッカーが機能するのはコール/フォールドのボーダーライン（後手スコア±2程度）のコンボに対してのみ
> - 限界1: レンジの一部しか削れない。限界2: ストーリー整合性がブロッカーより優先。限界3: 相手のプレイスタイルによって効果が変わる
> - 巻� 第12章以降では、α式・後手スコア・ブロッカーの3要素を統合した実戦判断を展開する

---

**【GTOとのズレ】**

GTOソルバーはコンボ単位で期待値を計算し、ブロッカー効果を内包した形で最適なアクションを導出します。人間がブロッカーを使って同じ精度を再現するのは不可能ですが、実戦で最も顕著にGTOとズレる点を2つ挙げます。

最大のズレは**「ブロッカーがあるから方針を変える」という逆転**です。α式で「この局面ではブラフ余地がない」と判断した後でも、「ナッツブロッカーを持つから打てるはず」という思考でブラフを実行するプレイヤーがいます。GTOはα式に相当する配分設計を先に完了させた上でコンボを選びます。配分なきブロッカー選択は、単にバリュー/ブラフのバランスを崩すだけです。ブロッカーは方針を変えない、という原則は実戦で最も守りにくいルールです。

次のズレは**ブラフブロッカーの見落とし**です。コール判断で「相手のバリューをブロックしているか」は意識しやすい一方で、「自分が相手のブラフを削っているか（ブラフブロッカー）」は見落とされやすいです。外れたフラッシュドローのキーカードや、外れたOESDのキーカードを自分が持つ場合、相手のブラフが物理的に減少しており、コールの期待値が下がります。GTOソルバーはこの両方向のブロッカー効果を正確に計算しますが、人間は一方向（バリューブロッカー）に注目しがちです。

また、**バリューをブラフとして使う誤り**も実戦で頻繁に起きます。「このカードはブロッカーが強いからブラフとして使える」と判断した際に、実はそのハンドがショーダウン価値を持つバリューハンドだった場合です。役の見落とし（特にリバーでのブロードウェイストレートの完成、ペアボードでの2ペア昇格等）がこの誤りの原因になります。ブラフ候補を選ぶ前に必ずHandScoreを計算し、バリューのハンドをブラフとして使わないことが最優先です。
